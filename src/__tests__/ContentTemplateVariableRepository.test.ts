
import { ContentTemplateVariableRepository } from '../lib/repositories/ContentTemplateVariableRepository';
import { ContentTemplateVariableInterface } from '../types/nodeTypes';

describe('ContentTemplateVariableRepository', () => {
  it('should be defined', () => {
    expect(ContentTemplateVariableRepository).toBeDefined();
  });

  // Add more tests as needed
});
