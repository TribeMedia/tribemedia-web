
import { PromptTemplateRepository } from '../lib/repositories/PromptTemplateRepository';
import { PromptTemplateInterface } from '../types/nodeTypes';

describe('PromptTemplateRepository', () => {
  it('should be defined', () => {
    expect(PromptTemplateRepository).toBeDefined();
  });

  // Add more tests as needed
});
