
import { PromptVariableValueRepository } from '../lib/repositories/PromptVariableValueRepository';
import { PromptVariableValueInterface } from '../types/nodeTypes';

describe('PromptVariableValueRepository', () => {
  it('should be defined', () => {
    expect(PromptVariableValueRepository).toBeDefined();
  });

  // Add more tests as needed
});
