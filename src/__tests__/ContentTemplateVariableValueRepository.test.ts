
import { ContentTemplateVariableValueRepository } from '../lib/repositories/ContentTemplateVariableValueRepository';
import { ContentTemplateVariableValueInterface } from '../types/nodeTypes';

describe('ContentTemplateVariableValueRepository', () => {
  it('should be defined', () => {
    expect(ContentTemplateVariableValueRepository).toBeDefined();
  });

  // Add more tests as needed
});
