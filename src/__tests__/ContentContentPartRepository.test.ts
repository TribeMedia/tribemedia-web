
import { ContentContentPartRepository } from '../lib/repositories/ContentContentPartRepository';
import { ContentContentPartInterface } from '../types/nodeTypes';

describe('ContentContentPartRepository', () => {
  it('should be defined', () => {
    expect(ContentContentPartRepository).toBeDefined();
  });

  // Add more tests as needed
});
