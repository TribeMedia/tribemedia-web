
import { ContentTypeRepository } from '../lib/repositories/ContentTypeRepository';
import { ContentTypeInterface } from '../types/nodeTypes';

describe('ContentTypeRepository', () => {
  it('should be defined', () => {
    expect(ContentTypeRepository).toBeDefined();
  });

  // Add more tests as needed
});
