
import { ContentPartRepository } from '../lib/repositories/ContentPartRepository';
import { ContentPartInterface } from '../types/nodeTypes';

describe('ContentPartRepository', () => {
  it('should be defined', () => {
    expect(ContentPartRepository).toBeDefined();
  });

  // Add more tests as needed
});
