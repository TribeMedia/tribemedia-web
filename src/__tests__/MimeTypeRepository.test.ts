
import { MimeTypeRepository } from '../lib/repositories/MimeTypeRepository';
import { MimeTypeInterface } from '../types/nodeTypes';

describe('MimeTypeRepository', () => {
  it('should be defined', () => {
    expect(MimeTypeRepository).toBeDefined();
  });

  // Add more tests as needed
});
