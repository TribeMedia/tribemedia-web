
import { ContentTemplateRepository } from '../lib/repositories/ContentTemplateRepository';
import { ContentTemplateInterface } from '../types/nodeTypes';

describe('ContentTemplateRepository', () => {
  it('should be defined', () => {
    expect(ContentTemplateRepository).toBeDefined();
  });

  // Add more tests as needed
});
