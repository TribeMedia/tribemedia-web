
import { TemplateVariableRepository } from '../lib/repositories/TemplateVariableRepository';
import { TemplateVariableInterface } from '../types/nodeTypes';

describe('TemplateVariableRepository', () => {
  it('should be defined', () => {
    expect(TemplateVariableRepository).toBeDefined();
  });

  // Add more tests as needed
});
