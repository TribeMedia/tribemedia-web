
import { AssetTypeRepository } from '../lib/repositories/AssetTypeRepository';
import { AssetTypeInterface } from '../types/nodeTypes';

describe('AssetTypeRepository', () => {
  it('should be defined', () => {
    expect(AssetTypeRepository).toBeDefined();
  });

  // Add more tests as needed
});
