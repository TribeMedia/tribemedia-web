
import { ContentPartContentPartRepository } from '../lib/repositories/ContentPartContentPartRepository';
import { ContentPartContentPartInterface } from '../types/nodeTypes';

describe('ContentPartContentPartRepository', () => {
  it('should be defined', () => {
    expect(ContentPartContentPartRepository).toBeDefined();
  });

  // Add more tests as needed
});
