
import { ApplicationTypeRepository } from '../lib/repositories/ApplicationTypeRepository';
import { ApplicationTypeInterface } from '../types/nodeTypes';

describe('ApplicationTypeRepository', () => {
  it('should be defined', () => {
    expect(ApplicationTypeRepository).toBeDefined();
  });

  // Add more tests as needed
});
