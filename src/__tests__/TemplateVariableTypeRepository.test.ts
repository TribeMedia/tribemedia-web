
import { TemplateVariableTypeRepository } from '../lib/repositories/TemplateVariableTypeRepository';
import { TemplateVariableTypeInterface } from '../types/nodeTypes';

describe('TemplateVariableTypeRepository', () => {
  it('should be defined', () => {
    expect(TemplateVariableTypeRepository).toBeDefined();
  });

  // Add more tests as needed
});
