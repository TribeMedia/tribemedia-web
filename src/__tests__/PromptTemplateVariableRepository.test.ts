
import { PromptTemplateVariableRepository } from '../lib/repositories/PromptTemplateVariableRepository';
import { PromptTemplateVariableInterface } from '../types/nodeTypes';

describe('PromptTemplateVariableRepository', () => {
  it('should be defined', () => {
    expect(PromptTemplateVariableRepository).toBeDefined();
  });

  // Add more tests as needed
});
