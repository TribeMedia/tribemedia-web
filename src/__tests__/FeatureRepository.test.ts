
import { FeatureRepository } from '../lib/repositories/FeatureRepository';
import { FeatureInterface } from '../types/nodeTypes';

describe('FeatureRepository', () => {
  it('should be defined', () => {
    expect(FeatureRepository).toBeDefined();
  });

  // Add more tests as needed
});
